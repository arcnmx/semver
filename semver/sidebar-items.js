initSidebarItems({"enum":[["Identifier","An identifier in the pre-release or build metadata."],["ParseError","A `ParseError` is returned as the `Err` side of a `Result` when a version is attempted to be parsed."],["ReqParseError","A `ReqParseError` is returned from methods which parse a string into a `VersionReq`. Each enumeration is one of the possible errors that can occur."]],"struct":[["Version","Represents a version number conforming to the semantic versioning scheme."],["VersionReq","A `VersionReq` is a set of version comparator sets; it corresponds to the top-level \"version range\" in the Npm implementation of SemVer:   https://docs.npmjs.com/misc/semver#ranges"],["VersionSet","`VersionSet` is composed of a set of one or more comparators (predicates). A specific version can then be matched against the range to see if it satisfies the constraints set by the predicates."]]});